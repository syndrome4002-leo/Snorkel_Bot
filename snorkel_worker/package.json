{
  "name": "snorkel-bot-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Picks up Snorkel tasks from Firestore and works them with the Claude Code agent.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node scripts/check.mjs",
    "once": "node scripts/run-one.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.7.0"
  }
}
