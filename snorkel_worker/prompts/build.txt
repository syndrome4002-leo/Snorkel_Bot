Below is task infos from the snorkel platform:
===================================================

{{initial_infos}}

==================================================================================================

The downloaded task files are in this directory. Inspect them, decide what kind
of task this is, and then do whatever that decision calls for — all in this one
reply. Do not stop to check the decision with me first.

Start with the judgement on a line of its own: fixable, invalid, or valid-as-is.
Then a short paragraph saying why, in a human tone, with no markdown and no
bullet points.

Then, if the judgement is FIXABLE, make the corrections in this task directory.

Do not build a zip, and do not copy instruction.md over
environment/problem_statement.md. Both are done for you after this session, from
whatever the folder holds at the end — so the folder is the deliverable, and
every minute spent zipping, unzipping to check it, and deleting the last attempt
is a minute spent on something that gets redone anyway.

Do not keep a todo list. Nothing reads it.

If the judgement is INVALID or VALID-AS-IS, there is nothing to correct and
nothing to upload. Leave the files as they are — the form below is the whole of
what is left to do, and it is the only thing the reviewer will read.

Writing answers rule:
- Each answer should be not so long, just like human-written.
- Never wrap a sentence onto the next line. A paragraph is one line however long
  it runs; only a real break between parts or items is a new line.
- The answers must not include hypen links or bullet points, symbols, formatting
  patterns including "—"
- In an answer field, there should be a blank line between each item. ( apply
  only for answers that have multiple items )

In the answer describing each issue, the category in brackets is one you selected
above, copied exactly as written there — "[the instructions are
overly-prescriptive]", not "[Instructions / Overly prescriptive]" — on a line of
its own, with the description starting on the next line. An environment problem
gets its own item headed [environment/dockerfile].

Every numbered item needs all of what the question asks for: the category, the
explanation, whether it is fixable, and how it was fixed — each of those on one
line, however long it runs. Never wrap a sentence onto the next line: the line
break is what separates one part from the next.

So a complete item reads — four lines, a blank line, then the next item:

1) [the instructions are overly-prescriptive]
The instruction named the exact status strings and field names the tests assert on, which tells the agent what to write instead of what the code should do.
This is fixable.
Rewritten to describe the behaviour, keeping the contract vocabulary but not the test assertions.

2) [environment/dockerfile]
task.toml was missing os, still carried network_mode in all three sections, and had no difficulty_explanation.
This is fixable.
Added os, removed the network fields, added difficulty_explanation.
